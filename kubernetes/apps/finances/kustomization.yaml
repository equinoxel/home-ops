---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: finances

components:
  - ../../components/common
  - ../../components/sops
  - ../../components/repos/app-template
resources:
  - ./actual-budget
  # - ./sure-am - It is too slow for my cluster. Also Enable Banking fails.
