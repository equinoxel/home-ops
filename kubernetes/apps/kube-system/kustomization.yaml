---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kube-system

components:
  - ../../components/sops

resources:
  - ./namespace.yaml
  - ./cilium/ks.yaml
  - ./coredns/ks.yaml
  - ./metrics-server/ks.yaml
  - ./reloader/ks.yaml
  # spegel needs >1 node to bootstrap its P2P routing table; disabled while
  # single-node. Re-enable once a second node is added.
  # - ./spegel/ks.yaml
